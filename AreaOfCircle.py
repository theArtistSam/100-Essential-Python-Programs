# Python program to calculate the Area of a Circle

Radius = eval(input("Enter the value of Radius: "))  # Prompt the user to enter the value of Radius and store it in 'Radius'
Area = Radius * Radius * 3.1459  # Calculate the area using the formula and assign the result to 'Area'
print(Area)  # Print the calculated area
