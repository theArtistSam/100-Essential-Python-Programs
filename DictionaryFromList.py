# Define keys and values as lists
keys = ['red', 'green', 'blue']
values = ['#FF0000', '#008000', '#0000FF']

# Create a dictionary by zipping keys and values
color_dictionary = dict(zip(keys, values))

# Print the color dictionary
print(color_dictionary)

print("\n")
